import 'package:flutter/material.dart';

class BackPreviousScreenBtn extends StatelessWidget {
  const BackPreviousScreenBtn({super.key});

  @override
  Widget build(BuildContext context) {
    return BackButton(
      color: Colors.white,
    );
  }
}